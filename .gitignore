# Dependencies and build output (rebuilt automatically on every deploy)
node_modules/
dist/
.astro/

# macOS
.DS_Store

# Local assistant/editor settings (preview config etc.)
.claude/

# Private working notes: never commit them (this repository is public)
NOTES.md
/.playwright-mcp
